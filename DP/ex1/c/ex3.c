#include <stdio.h>

main(){
	int a[10];
	unsigned char b[]={1,2,3,4};


	/*
	 * Arrays are also pointers.
    * 1) Find out the address of a, b, a[0], and b[0]
    *
	 * 2) Find out where a[1] and b[1] are located (address).
	 * What about the delta between a[0] and a[1] resp. b[0] and b[1]?
	 * 3) Calculate the sum of the values in b without using the square braces '[' or ']'.
	 * use instead the * operator and pointer arithmetic.
	 */
 



}



